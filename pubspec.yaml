name: limited_wrap

description: "library contains widget that extends the standard `Wrap` functionality with line limiting capabilities and a customizable button."

version: 1.0.2

repository: https://github.com/red-collar-official/limited_wrap
issue_tracker: https://github.com/red-collar-official/limited_wrap/issues
documentation: https://github.com/red-collar-official/limited_wrap/blob/main/README.md

topics: [wrap, ui] 

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: